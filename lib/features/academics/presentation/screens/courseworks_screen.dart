import 'package:flutter/material.dart';

class CourseworksScreen extends StatelessWidget {
  static String id = 'courseworks';

  const CourseworksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Courseworks')),
    );
  }
}
